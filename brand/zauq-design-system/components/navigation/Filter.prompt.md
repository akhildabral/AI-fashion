Narrows a set — closet categories, journal date ranges.

```jsx
<Filter on={cat === 'top'} onClick={() => setCat('top')} count={12}>Tops</Filter>
```

Deliberately never brass: brass means "act" or "you are here", and a row of eight brass filters would drown both. Separate groups with a 1px × 16px ink rule at 15%.
