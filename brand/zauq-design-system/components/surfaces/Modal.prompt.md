The one detail surface — piece stories, reconsider sheets, let-go confirmations.

```jsx
<Modal open={open} onClose={() => setOpen(false)} title="Camel blazer">
  <Arch aspect="5/6">…</Arch>
</Modal>
```

ZAUQ has no bottom sheets on desktop and no nested modals. The scrim is 40% ink with a 2px blur; the panel enters on `.zq-rise` and takes the float shadow.
