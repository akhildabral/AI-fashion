The text input, paired with `Label` above it.

```jsx
<div>
  <Label htmlFor="email">Email</Label>
  <Field id="email" type="email" placeholder="you@example.com" />
</div>
```

Sits at 44px so it lines up with `Button` in a row — the landing page's waitlist form is exactly a `Field` and a primary `Button` side by side. Never round the corners past 3px.
